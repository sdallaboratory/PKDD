import { UserColorPipe } from './user-color.pipe';

describe('UserColorPipe', () => {
  it('create an instance', () => {
    const pipe = new UserColorPipe();
    expect(pipe).toBeTruthy();
  });
});

import { StrongestRolePipe } from './strongest-role.pipe';

describe('StrongestRolePipe', () => {
  it('create an instance', () => {
    const pipe = new StrongestRolePipe();
    expect(pipe).toBeTruthy();
  });
});

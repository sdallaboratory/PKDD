import { ForRolesDirective } from './for-roles.directive';

describe('ForRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ForRolesDirective(null, null, null, null);
    expect(directive).toBeTruthy();
  });
});

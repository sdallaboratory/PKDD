import { MmpiScalePipe } from './mmpi-scale.pipe';

describe('MmpiScalePipe', () => {
  it('create an instance', () => {
    const pipe = new MmpiScalePipe();
    expect(pipe).toBeTruthy();
  });
});

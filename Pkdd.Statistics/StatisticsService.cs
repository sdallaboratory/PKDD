using Pkdd.Database;
using System;

namespace Pkdd.Statistics
{
    public class StatisticsService
    {
        private readonly PkddDbContext _context;

        public StatisticsService(
            PkddDbContext context
            )
        {
            _context = context;
        }

        LogAction()
    }
}
